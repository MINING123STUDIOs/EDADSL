Todo:

Fix if example to correctly use cardinality

Formalize ordering inside of *e

Add phy.align <list of parts> axis = <axis> s = <spacing> <priority>

Add phy.connmatch <property> <set of connections> <priority>

Allow tag assignment at net creation 

Allow log file creation

Add mkpcb and mkschematic command or something similar 

Add pcb flags.
    ?$vias true
    ?$slots false
For slots, vias, solder reinforced tracks, jumper wires, ect

Add surface plating option to phy.board? 

[<start>:<end>] is equivalent to [<start>:1:<end>]
Also add [<start>|<end>|<number of steps>]

Add language name to the top of the document 

Allow untaging

T and B layer references

More <quantifier>@<symbol><thing> stuff. It's neat I think 

Modes like phystrict or phylenient

Fix @^ in ssr example: adding C
