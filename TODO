Todo:

X Fix if example to correctly use cardinality

X Formalize ordering inside of *e

X Add phy.align <list of parts> axis = <axis> s = <spacing> <priority>

X Add phy.connmatch <property> <set of connections> <priority>

X Allow tag assignment at net creation 

X Allow log file creation

X Add mkpcb and mkschematic command or something similar 

X Add pcb flags.
      ?$vias true
      ?$slots false
  For slots, vias, solder reinforced tracks, jumper wires, ect

X Add surface plating option to phy.board

X [<start>:<end>] is equivalent to [<start>:1:<end>]
  Also add [<start>|<end>|<number of steps>]

X Add language name to the top of the document 

X Allow untaging

O T and B layer references

O <primary quantifier>@<secondary quantifier>~<object> to return all objecs given by <primary quantifier> affiliated with the <object> whichs type is classified by <secondary quantifier>
      example:
      C@p~U2 # all connections affiliated with U2

O Modes like phystrict or phylenient

X Fix @^ in ssr example: adding E

O Add flags on what part-text to show on the pcb

O Add PCB text commands
