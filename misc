# ============================================================
# PROPOSED BUILT-IN OPERATORS
# ============================================================

# ------------------------------------------------------------
# NULL (if nullable values exist)
# ------------------------------------------------------------
a ?? b         Null coalescing
a?.member      Safe member access


# ------------------------------------------------------------
# MATRIX
# ------------------------------------------------------------
A.H            Hermitian (conjugate) transpose


# ============================================================
# PROPOSED BUILT-IN CONSTANTS
# ============================================================

# ------------------------------------------------------------
# Mathematical
# ------------------------------------------------------------
c.tau          2π
c.sqrtpi       √π
c.ln10         ln(10)
c.log2e        log₂(e)
c.log10e       log₁₀(e)

# ------------------------------------------------------------
# Physical
# ------------------------------------------------------------
c.Na           Avogadro constant
c.R            Ideal gas constant
c.F            Faraday constant
c.muB          Bohr magneton
c.muN          Nuclear magneton
c.a0           Bohr radius

# ------------------------------------------------------------
# Electrical / Quantum Electronics
# ------------------------------------------------------------
c.Z0           Vacuum impedance (~376.730313668 Ω)
c.RK           von Klitzing constant
c.KJ           Josephson constant

# ------------------------------------------------------------
# Floating-point
# ------------------------------------------------------------
c.NaN          Not-a-Number


# ============================================================
# PROPOSED PSEUDO-CONSTANTS
# ============================================================

*v              All nets
*fp             All footprints
*pkg            All packages
*layer          All board layers



# ============================================================
# MATH FUNCTIONS
# ============================================================

clamp(x, min, max)       Limit value to range

trunc(x)

cbrt(x)

log2(x)

hypot(a, b)              sqrt(a²+b²)

factorial(n)
gamma(x)
lgamma(x)


# ============================================================
# COMPLEX FUNCTIONS
# ============================================================


phase(z)                 Phase angle

arg(z)                   Argument


# ============================================================
# RANDOM / STATISTICS
# ============================================================

random()

randInt(min, max)
randReal(min, max)

median(list)
mode(list)

normalize(list)


# ============================================================
# LIST / ARRAY FUNCTIONS
# ============================================================

push(list, item)
pop(list)

head(list, n)
tail(list, n)

unique(list)

flatten(list)

transpose(list)

reshape(array, shape)

zeros(shape)
ones(shape)
identity(n)


# ============================================================
# MATRIX / LINEAR ALGEBRA
# ============================================================

det(A)                   Determinant

inverse(A)

trace(A)

rank(A)

eigen(A)

svd(A)

solve(A, b)

norm(A)

condition(A)


# ============================================================
# ENGINEERING FUNCTIONS
# ============================================================

db(x)                    Convert magnitude to dB
db10(x)                  10log10(x)
db20(x)                  20log10(x)

fromDb(x)

period(freq)

frequency(period)


# ============================================================
# ELECTRONICS FUNCTIONS
# ============================================================

series(a, b, ...)

voltageDivider(r1, r2, vin)

currentDivider(r1, r2, i)

rcTime(r, c)

rlTime(r, l)

lcFrequency(l, c)

resonanceFrequency(l, c)

impedance(r, x)

reactance(freq, component)



# ============================================================
# PCB / EDA FUNCTIONS
# ============================================================

is_part(x)
is_pin(x)
is_net(x)
is_connection(x)

connected(a, b)

neighbors(x)

nets(part)

parts(net)

pins(part)

findPart(name)

findNet(name)

findPin(name)


# ============================================================
# FILTER / QUERY FUNCTIONS
# ============================================================

foreach(collection, function)

count(collection, condition)

exists(collection, condition)

all(collection, condition)


# ============================================================
# STRING FUNCTIONS
# ============================================================

contains(str, sub)

startsWith(str, prefix)
endsWith(str, suffix)

split(str, delimiter)
join(list, delimiter)

replace(str, old, new)

regexMatch(str, pattern)


# ============================================================
# UNIT FUNCTIONS
# ============================================================

convert(value, unit)

unit(value)

dimension(value)

isCompatible(unit1, unit2)


# ============================================================
# TIME FUNCTIONS
# ============================================================

now()

sleep(ms)

timerStart()

timerStop()


# ============================================================
# FILE / DATA FUNCTIONS
# ============================================================

readFile(path)

writeFile(path, data)

readCSV(path)

writeCSV(path, data)

parseJSON(str)

formatJSON(obj)



# ============================================================
# STRING FUNCTIONS
# ============================================================

# ------------------------------------------------------------
# Length / Access
# ------------------------------------------------------------

length(str)                 Get character count

charAt(str, index)          Get character at index

codeAt(str, index)          Get Unicode code point

substring(str, start, end)  Extract substring

slice(str, start, end)      Extract substring (supports negatives)


# ------------------------------------------------------------
# Searching
# ------------------------------------------------------------

contains(str, sub)          Check if substring exists

indexOf(str, sub)           First occurrence index

lastIndexOf(str, sub)       Last occurrence index

find(str, sub)              Alias for indexOf

count(str, sub)             Count occurrences

startsWith(str, prefix)

endsWith(str, suffix)


# ------------------------------------------------------------
# Modification
# ------------------------------------------------------------

replace(str, old, new)

replaceAll(str, old, new)

insert(str, index, value)

remove(str, start, end)

repeat(str, n)

reverse(str)


# ------------------------------------------------------------
# Case Conversion
# ------------------------------------------------------------

toUpperCase(str)

toLowerCase(str)

capitalize(str)

titleCase(str)

swapCase(str)


# ------------------------------------------------------------
# Whitespace
# ------------------------------------------------------------

trim(str)

trimStart(str)

trimEnd(str)

padLeft(str, length, char)

padRight(str, length, char)


# ------------------------------------------------------------
# Splitting / Joining
# ------------------------------------------------------------

split(str, delimiter)

splitLines(str)

join(list, delimiter)

partition(str, separator)


# ------------------------------------------------------------
# Formatting
# ------------------------------------------------------------

format(str, args...)

printf(format, args...)

formatNumber(value, digits)

formatHex(value, width)

formatBinary(value, width)


# ------------------------------------------------------------
# Validation
# ------------------------------------------------------------

isEmpty(str)

isBlank(str)

isNumeric(str)

isInteger(str)

isReal(str)

isIdentifier(str)

isValidUTF8(str)


# ------------------------------------------------------------
# Regex
# ------------------------------------------------------------

regexMatch(str, pattern)

regexFind(str, pattern)

regexFindAll(str, pattern)

regexReplace(str, pattern, replacement)


# ------------------------------------------------------------
# Encoding
# ------------------------------------------------------------

toBytes(str)

fromBytes(bytes)

base64Encode(data)

base64Decode(str)

hexEncode(data)

hexDecode(str)


# ------------------------------------------------------------
# EDA-Oriented String Functions
# ------------------------------------------------------------

parseReference(str)
# "R123" -> type="R", number=123

referenceType(str)
# "C47" -> "C"

referenceNumber(str)
# "R15" -> 15

normalizeName(str)
# "  r101 " -> "R101"

naturalSortKey(str)
# Makes R2 sort before R10

wildcardMatch(str, pattern)
# "R*" matches "R1", "R2", ...


# ------------------------------------------------------------
# Identifier / Symbol Functions
# ------------------------------------------------------------

isUpper(str)

isLower(str)

isAlpha(str)

isDigit(str)

isAlphanumeric(str)

sanitizeIdentifier(str)
# Converts arbitrary text into valid identifier





# ============================================================
# STRING SLICING
# ============================================================

# Strings support indexing and slicing using [] syntax.
# Slicing returns a new string and does not modify the original.

# ------------------------------------------------------------
# Single Character Indexing
# ------------------------------------------------------------

str[index]

Examples:

"hello"[0]       #c# = "h"
"hello"[1]       #c# = "e"
"hello"[4]       #c# = "o"

# Negative indices count from the end:

"hello"[-1]      #c# = "o"
"hello"[-2]      #c# = "l"


# ------------------------------------------------------------
# Slice Syntax
# ------------------------------------------------------------

str[start:end]

# Returns characters from start (inclusive)
# to end (exclusive).

Examples:

"hello"[0:2]     #c# = "he"

"hello"[1:4]     #c# = "ell"

"hello"[:3]      #c# = "hel"

"hello"[2:]      #c# = "llo"

"hello"[:]       #c# = "hello"


# ------------------------------------------------------------
# Negative Slice Indices
# ------------------------------------------------------------

# Negative values count from the end.

"hello"[-3:]     #c# = "llo"

"hello"[:-1]     #c# = "hell"

"hello"[-4:-1]   #c# = "ell"


# ------------------------------------------------------------
# Slice Step
# ------------------------------------------------------------

str[start:end:step]

# step controls the direction and spacing.

Examples:

"abcdef"[0:6:2]     #c# = "ace"

"abcdef"[1:6:2]     #c# = "bdf"

"abcdef"[::-1]      #c# = "fedcba"

"abcdef"[5:0:-1]    #c# = "fedcb"


# ------------------------------------------------------------
# Rules
# ------------------------------------------------------------

# start defaults to 0.
# end defaults to string length.
# step defaults to 1.

# Step cannot be zero.

"hello"[::0]
#c# raises Value Error:
#c# Slice step cannot be zero


# ------------------------------------------------------------
# Bounds Handling
# ------------------------------------------------------------

# Slice bounds are automatically clamped.

"hello"[0:100]
#c# = "hello"

"hello"[-100:3]
#c# = "hel"


# ------------------------------------------------------------
# Type Rules
# ------------------------------------------------------------

# Indexing requires an integer.

"hello"[1.5]
#c# raises Type Error


# Slice indices require integers.

"hello"[1.0:3]
#c# raises Type Error


# ------------------------------------------------------------
# Assignment
# ------------------------------------------------------------

# Strings are immutable.
# Slice assignment is not supported.

$str [string]= "hello"

$str[0] = "H"
#c# raises Type Error:
#c# Strings are immutable


# ------------------------------------------------------------
# Interaction With Existing Functions
# ------------------------------------------------------------

# Equivalent operations:

substring("hello", 1, 4)
#c# same as:
"hello"[1:4]


slice("hello", 1, 4)
#c# same as:
"hello"[1:4]


# ------------------------------------------------------------
# Unicode Handling
# ------------------------------------------------------------

# Indices operate on Unicode code points, not bytes.

"λx"[0]
#c# = "λ"

"λx"[1]
#c# = "x"




# ============================================================
# INPUT / OUTPUT (I/O) FUNCTIONS
# ============================================================

# ============================================================
# CONSOLE INPUT
# ============================================================

input()
# Read one line from standard input.
# Returns string.

input(prompt)
# Display prompt and read one line.

inputInt(prompt)
# Read and parse integer input.

inputReal(prompt)
# Read and parse real input.

inputBool(prompt)
# Read boolean input.

inputChoice(prompt, choices)
# Read a value selected from a list of choices.


# ============================================================
# CONSOLE OUTPUT
# ============================================================

print(value)
# Print value with newline.

println(value)
# Alias for print.

write(value)
# Print value without newline.

printf(format, args...)
# Formatted output.

format(value, options)
# Convert value to formatted string.


# ============================================================
# STANDARD STREAMS
# ============================================================

stdin
# Standard input stream.

stdout
# Standard output stream.

stderr
# Standard error stream.


readLine(stream)
# Read line from stream.

writeLine(stream, value)
# Write line to stream.

flush(stream)
# Flush output buffer.


# ============================================================
# COMMAND LINE ARGUMENTS
# ============================================================

args()
# Return command-line arguments.

argc()
# Number of arguments.

arg(index)
# Get argument by index.

arg(name)
# Get named argument.


Examples:

# tool script.edl board.pcb
args()[0]
# "board.pcb"


# ============================================================
# FILE I/O
# ============================================================

readFile(path)
# Read entire file as string.

writeFile(path, data)
# Write string to file.

appendFile(path, data)
# Append data to file.

exists(path)
# Check if file exists.

deleteFile(path)
# Delete file.

copyFile(source, destination)

moveFile(source, destination)


# ============================================================
# FILE HANDLES
# ============================================================

open(path, mode)

close(file)

read(file, size)

readLine(file)

write(file, data)

seek(file, position)

tell(file)
# Current position.

eof(file)
# End-of-file check.


# File modes:

"r"
# Read

"w"
# Write / truncate

"a"
# Append

"r+"
# Read/write


# ============================================================
# DIRECTORY FUNCTIONS
# ============================================================

createDirectory(path)

removeDirectory(path)

listDirectory(path)

existsDirectory(path)

currentDirectory()

changeDirectory(path)


# ============================================================
# PATH FUNCTIONS
# ============================================================

joinPath(parts...)

basename(path)

dirname(path)

extension(path)

normalizePath(path)

absolutePath(path)

relativePath(path, base)


# ============================================================
# TEXT FILE FUNCTIONS
# ============================================================

readLines(path)
# Returns list of strings.

writeLines(path, lines)

appendLines(path, lines)


# ============================================================
# CSV FUNCTIONS
# ============================================================

readCSV(path)

writeCSV(path, data)

parseCSV(text)

formatCSV(data)


# ============================================================
# JSON FUNCTIONS
# ============================================================

parseJSON(text)

formatJSON(object)

readJSON(path)

writeJSON(path, object)


# ============================================================
# YAML / CONFIG FUNCTIONS
# ============================================================

readConfig(path)

writeConfig(path, object)

parseYAML(text)

formatYAML(object)


# ============================================================
# BINARY DATA
# ============================================================

readBytes(path)

writeBytes(path, bytes)

byteLength(data)

toBytes(value)

fromBytes(bytes)


# ============================================================
# ENCODING
# ============================================================

base64Encode(data)

base64Decode(text)

hexEncode(data)

hexDecode(text)

utf8Encode(text)

utf8Decode(bytes)


# ============================================================
# SERIALIZATION
# ============================================================

serialize(object)

deserialize(data)

saveObject(path, object)

loadObject(path)


# ============================================================
# STREAMING
# ============================================================

createStream()

pipe(source, destination)

copyStream(source, destination)

streamAvailable(stream)


# ============================================================
# PROCESS I/O
# ============================================================

execute(command)

spawn(command)

wait(process)

kill(process)

processOutput(process)

processError(process)


# ============================================================
# LOGGING
# ============================================================

log(message)

logInfo(message)

logWarning(message)

logError(message)

logDebug(message)


setLogLevel(level)

"debug"
"info"
"warning"
"error"


# ============================================================
# EDA-SPECIFIC FILE I/O
# ============================================================

loadProject(path)

saveProject(path)

loadPCB(path)

savePCB(path)

loadSchematic(path)

saveSchematic(path)

loadNetlist(path)

saveNetlist(path)

loadBOM(path)

saveBOM(path)


# ============================================================
# EDA IMPORT / EXPORT
# ============================================================

importGerber(path)

exportGerber(path)

importDXF(path)

exportDXF(path)

importSPICE(path)

exportSPICE(path)


# ============================================================
# DATABASE I/O
# ============================================================

openDatabase(path)

closeDatabase(db)

query(db, sql)

insert(db, table, data)

update(db, table, data)

delete(db, table, condition)


# ============================================================
# NETWORK I/O
# ============================================================

httpGet(url)

httpPost(url, data)

download(url, path)

upload(path, url)

tcpConnect(host, port)

tcpSend(socket, data)

tcpReceive(socket)


# ============================================================
# SERIAL / HARDWARE I/O
# ============================================================

serialOpen(port, baud)

serialClose(port)

serialWrite(port, data)

serialRead(port)

gpioRead(pin)

gpioWrite(pin, value)


# ============================================================
# TIMING / BUFFERING
# ============================================================

sleep(ms)

delay(ms)

flush()

bufferCreate()

bufferClear()

bufferSize()



# ============================================================
# ENGINEERING / INTEGER UTILITY FUNCTIONS
# ============================================================

clamp(x, min, max)          # constrain value to range
lerp(a, b, t)               # linear interpolation

alignUp(x, alignment)       # round integer upward to boundary
alignDown(x, alignment)     # round integer downward to boundary

isPowerOfTwo(n)             # test power-of-two
nextPowerOfTwo(n)           # next power-of-two >= n
log2Int(n)                  # integer base-2 logarithm

popcount(n)                 # count set bits
parity(n)                   # XOR of all bits
bitLength(n)                # number of bits required



# ============================================================
# SIGNAL PROCESSING FUNCTIONS
# ============================================================

fft(signal)
ifft(signal)

convolve(a, b)
correlate(a, b)

window(type, size)

lowPass(signal, cutoff)
highPass(signal, cutoff)
bandPass(signal, low, high)


# ============================================================
# RANDOM / SIMULATION
# ============================================================

random()
randomInt(min, max)
randomReal(min, max)

seed(value)

choice(list)
shuffle(list)

normal(mean, stddev)
uniform(min, max)



# ============================================================
# HASH FUNCTIONS
# ============================================================

hash(x)                     # general-purpose hash value
hashBytes(bytes)            # hash raw byte data

# ============================================================
# NON-CRYPTOGRAPHIC HASHES
# ============================================================

fnv1a(data)                 # FNV-1a hash
murmurHash(data)            # MurmurHash
xxHash(data)                # fast non-cryptographic hash
crc32(data)                 # CRC-32 checksum
adler32(data)               # Adler-32 checksum

# ============================================================
# CRYPTOGRAPHIC HASHES
# ============================================================

md5(data)                   # MD5 digest (legacy)
sha1(data)                  # SHA-1 digest (legacy)

sha224(data)                # SHA-224 digest
sha256(data)                # SHA-256 digest
sha384(data)                # SHA-384 digest
sha512(data)                # SHA-512 digest

sha3_256(data)              # SHA-3 256-bit digest
sha3_512(data)              # SHA-3 512-bit digest

blake2b(data)               # BLAKE2b digest
blake2s(data)               # BLAKE2s digest
blake3(data)                # BLAKE3 digest


# ============================================================
# HASH OUTPUT FORMATS
# ============================================================

hash(data, "hex")           # hexadecimal string
hash(data, "bytes")         # byte array
hash(data, "int")           # integer value


# ============================================================
# POSSIBLE OPERATORS
# ============================================================

??              # null/coalesce operator

|>              # pipeline operator

..              # range creation

in              # membership alternative

?.              # safe access

:=              # immutable binding

=>              # pattern/lambda shorthand



log(a, b)

ilog2(n)          # integer floor(log2(n))
ceilLog2(n)       # smallest integer k where 2^k >= n


# ============================================================
# BASE CONVERSION FUNCTIONS
# ============================================================

toBase(value, base)              # convert integer to string in base
fromBase(string, base)           # parse string in base to integer

base(value)                      # get current/default representation
convertBase(value, from, to)     # convert between bases

# Common shortcuts

bin(value)                       # binary representation
oct(value)                       # octal representation
hex(value)                       # hexadecimal representation

fromBin(string)
fromOct(string)
fromHex(string)

# ============================================================
# SPECIAL FUNCTIONS
# ============================================================

# ============================================================
# Lambert W FUNCTION
# ============================================================

lambertW(x)                 # principal branch W0(x)
lambertW(x, branch)         # specify branch

# Solves:
# W(x) * e^W(x) = x

# Examples:
lambertW(0)
#c# = 0

lambertW(e)
#c# ≈ 1

lambertW(-1/e)
#c# = -1 (branch dependent)


# ============================================================
# POLYLOGARITHMS
# ============================================================

Li2(x)                      # dilogarithm
Li3(x)                      # trilogarithm
Li(n, x)                    # general polylogarithm

# Definition:
# Li_s(x) = Σ(k=1 to infinity) x^k / k^s


# ============================================================
# GAMMA / RELATED FUNCTIONS
# ============================================================

gamma(x)                    # Gamma function Γ(x)
logGamma(x)                 # ln(Γ(x))
digamma(x)                  # ψ(x)
trigamma(x)                 # ψ'(x)

beta(a, b)                  # Beta function


# ============================================================
# ERROR FUNCTIONS
# ============================================================

erf(x)                      # error function
erfc(x)                     # complementary error function
erfi(x)                     # imaginary error function


# ============================================================
# BESSEL FUNCTIONS
# ============================================================

besselJ(n, x)               # first kind
besselY(n, x)               # second kind
besselI(n, x)               # modified first kind
besselK(n, x)               # modified second kind


# ============================================================
# LEGENDRE FUNCTIONS
# ============================================================

legendreP(n, x)
legendreQ(n, x)

associatedLegendre(l, m, x)


# ============================================================
# ORTHOGONAL POLYNOMIALS
# ============================================================

hermite(n, x)
laguerre(n, x)
chebyshev(n, x)


# ============================================================
# CONVOLUTION / CORRELATION
# ============================================================

conv.*

conv.convolve(a, b)
    # discrete convolution
    # y[n] = Σ a[k]b[n-k]

conv.deconvolve(a, b)
    # inverse convolution / estimate original signal

conv.correlate(a, b)
    # cross-correlation

conv.autocorrelate(a)
    # signal correlated with itself

conv.xcorr(a, b)
    # normalized cross-correlation

conv.convolve2D(image, kernel)
    # 2D convolution

conv.convolveND(array, kernel)
    # N-dimensional convolution


# ============================================================
# TYPE / VALUE INTROSPECTION
# ============================================================

typeOf(x)
# Returns the runtime type name of a value.

isType(x, type)
# Checks whether a value matches a specified type.

isNull(x)
# Returns true if the value is null.

isDefined(x)
# Checks whether a variable or value exists.

isCallable(x)
# Checks whether a value can be called as a function.

sizeof(x)
# Returns the memory size of an object.

sizeofType(type)
# Returns the memory size required for a type.

clone(x)
# Creates a deep copy of an object.

copy(x)
# Creates a shallow copy of an object.


# ============================================================
# NUMERICAL ANALYSIS
# ============================================================

derivative(f, x)
# Calculates the derivative of function f at point x.

gradient(f, point)
# Calculates the gradient vector of a multivariable function.

jacobian(f, point)
# Calculates the Jacobian matrix of a vector function.

integrate(f, a, b)
# Computes the definite integral of f from a to b.

integrateSimpson(f, a, b)
# Performs numerical integration using Simpson's rule.

integrateMonteCarlo(f, bounds)
# Estimates an integral using Monte Carlo sampling.

root(f, guess)
# Finds a root of a function near an initial guess.

roots(polynomial)
# Finds all roots of a polynomial.

minimize(f, start)
# Finds the minimum value of a function.

maximize(f, start)
# Finds the maximum value of a function.

interpolate(x, y, value)
# Estimates a value between known data points.

spline(x, y)
# Creates a spline interpolation function.


# ============================================================
# OPTIMIZATION
# ============================================================

linearProgram(A, b, c)
# Solves a linear programming optimization problem.

quadraticProgram(Q, c)
# Solves a quadratic optimization problem.

leastSquares(A, b)
# Computes the least-squares solution of a system.

gradientDescent(f, initial)
# Minimizes a function using gradient descent.

newtonMethod(f, initial)
# Finds solutions using Newton's iterative method.

geneticOptimize(f, bounds)
# Optimizes a function using a genetic algorithm.


# ============================================================
# STATISTICS
# ============================================================

covariance(a, b)
# Measures how two datasets vary together.

correlation(a, b)
# Calculates normalized linear correlation.

skewness(list)
# Measures distribution asymmetry.

kurtosis(list)
# Measures distribution tail behavior.

quantile(list, q)
# Returns the value at quantile q.

histogram(list, bins)
# Creates a frequency distribution.

confidenceInterval(data, confidence)
# Calculates a statistical confidence interval.

zscore(x, mean, stddev)
# Converts a value into standard score form.

normalPDF(x, mean, stddev)
# Calculates the normal probability density function.

normalCDF(x, mean, stddev)
# Calculates the normal cumulative distribution function.

poisson(lambda, k)
# Calculates Poisson probability.

binomial(n, p, k)
# Calculates binomial probability.


# ============================================================
# UNITS / DIMENSIONS
# ============================================================

unitAdd(a, b)
# Adds two compatible physical quantities.

unitMultiply(a, b)
# Multiplies quantities while combining dimensions.

unitDivide(a, b)
# Divides quantities while combining dimensions.

parseUnit(string)
# Converts a unit string into a unit object.

quantity(value, unit)
# Creates a physical quantity with units.

convertTemperature(x, from, to)
# Converts between temperature scales.

isDimensionless(x)
# Checks whether a quantity has no units.


# ============================================================
# PHYSICAL QUANTITY EXAMPLES
# ============================================================

v = 5V
# Creates a voltage quantity.

R = 10kΩ
# Creates a resistance quantity.

I = v / R
# Automatically calculates current using dimensional arithmetic.


# ============================================================
# ELECTRICAL ENGINEERING
# ============================================================

ohmsLaw(v, r, i)
# Calculates voltage, resistance, or current using Ohm's law.

power(v, i)
# Calculates electrical power.

jouleEnergy(power, time)
# Calculates energy from power over time.

capacitorEnergy(c, voltage)
# Calculates stored capacitor energy.

inductorEnergy(l, current)
# Calculates stored inductor energy.

thermalResistance(power, temperature)
# Calculates thermal resistance.

voltageGain(input, output)
# Calculates voltage gain ratio.

currentGain(input, output)
# Calculates current gain ratio.

attenuation(db)
# Converts attenuation values.


# ============================================================
# CIRCUIT ANALYSIS
# ============================================================

nodalAnalysis(circuit)
# Performs node voltage analysis.

meshAnalysis(circuit)
# Performs mesh current analysis.

acSweep(circuit, start, stop)
# Performs AC frequency sweep simulation.

dcSweep(circuit, variable)
# Performs DC parameter sweep.

transient(circuit, time)
# Performs time-domain circuit simulation.

frequencyResponse(circuit)
# Calculates frequency response.

bodePlot(circuit)
# Generates Bode magnitude and phase data.

poleZero(circuit)
# Calculates system poles and zeros.

# ============================================================
# PCB GEOMETRY
# ============================================================

distance(a, b)
# Calculates the geometric distance between two points or objects.

angle(a, b, c)
# Calculates the angle formed by three points.

area(shape)
# Calculates the area of a geometric shape.

perimeter(shape)
# Calculates the boundary length of a shape.

intersects(a, b)
# Checks whether two geometric objects overlap or intersect.

contains(shape, point)
# Checks whether a shape contains a point.

boundingBox(object)
# Returns the smallest rectangle containing an object.

center(object)
# Returns the center point of an object.

rotate(object, angle)
# Rotates an object around its origin.

translate(object, dx, dy)
# Moves an object by a specified offset.

mirror(object, axis)
# Mirrors an object across an axis.


# ============================================================
# PCB DESIGN RULE CHECKING (DRC)
# ============================================================

checkDRC(board)
# Runs all configured design rule checks on a PCB.

checkClearance(net1, net2)
# Checks spacing between two electrical nets.

checkTraceWidth(net)
# Verifies trace widths meet design requirements.

checkViaSize(via)
# Checks whether a via matches allowed dimensions.

checkUnconnectedPins(board)
# Finds pins that are not connected.

checkFloatingNets(board)
# Detects nets without valid electrical connections.

checkShorts(board)
# Detects accidental electrical shorts.

checkAntenna(board)
# Detects antenna-rule violations on PCB traces.


# ============================================================
# PCB ROUTING
# ============================================================

route(net)
# Creates a route for a specified electrical net.

autoroute(board)
# Automatically routes PCB connections.

ripup(route)
# Removes an existing route.

optimizeTrace(trace)
# Optimizes trace geometry for performance or manufacturability.

lengthMatch(group)
# Adjusts trace lengths for timing matching.

calculateDelay(trace)
# Calculates propagation delay of a PCB trace.

calculateImpedance(trace)
# Calculates controlled impedance of a trace.


# ============================================================
# SIGNAL PROCESSING
# ============================================================

dft(signal)
# Calculates the discrete Fourier transform.

stft(signal)
# Calculates short-time Fourier transform.

hilbert(signal)
# Computes the Hilbert transform.

envelope(signal)
# Extracts signal amplitude envelope.

unwrapPhase(signal)
# Removes phase discontinuities.

resample(signal, rate)
# Changes the sampling rate of a signal.

interpolateSignal(signal)
# Estimates missing signal samples.

quantize(signal, bits)
# Converts a continuous signal into digital levels.

snr(signal, noise)
# Calculates signal-to-noise ratio.

thd(signal)
# Calculates total harmonic distortion.


# ============================================================
# CONTROL SYSTEMS
# ============================================================

transferFunction(num, den)
# Creates a transfer function from numerator and denominator coefficients.

pid(kp, ki, kd)
# Creates a PID controller model.

stepResponse(system)
# Calculates response to a step input.

impulseResponse(system)
# Calculates response to an impulse input.

rootLocus(system)
# Calculates root locus of a control system.

stateSpace(A, B, C, D)
# Creates a state-space system representation.

kalmanFilter(state)
# Creates or applies a Kalman filter for estimation.


# ============================================================
# IMAGE / CAD PROCESSING
# ============================================================

imageLoad(path)
# Loads an image file.

imageSave(image, path)
# Saves an image to a file.

edgeDetect(image)
# Detects edges in an image.

threshold(image, value)
# Converts an image into thresholded regions.

blur(image)
# Applies image blur filtering.

rotateImage(image, angle)
# Rotates an image.

crop(image, rect)
# Extracts a rectangular region from an image.

ocr(image)
# Performs optical character recognition.


# ============================================================
# MACHINE LEARNING
# ============================================================

normalizeFeatures(data)
# Normalizes numerical input features.

trainLinearModel(x, y)
# Trains a linear regression model.

predict(model, x)
# Generates predictions from a trained model.

kmeans(data, k)
# Performs k-means clustering.

pca(data)
# Performs principal component analysis.

confusionMatrix(actual, predicted)
# Creates a classification performance matrix.


# ============================================================
# DATE / TIME
# ============================================================

date()
# Returns the current calendar date.

time()
# Returns the current time.

year()
# Returns the current year.

month()
# Returns the current month.

day()
# Returns the current day.

weekday()
# Returns the day of the week.

timestamp()
# Returns the current Unix-style timestamp.

fromTimestamp(t)
# Converts a timestamp into a date/time object.

formatDate(date, pattern)
# Formats a date using a specified pattern.

parseDate(string)
# Converts a formatted string into a date object.


# ============================================================
# CONCURRENCY
# ============================================================

thread(fn)
# Runs a function in a separate thread.

parallel(fn)
# Executes a function using parallel processing.

async(fn)
# Starts an asynchronous operation.

await(task)
# Waits for an asynchronous task to complete.

mutex()
# Creates a mutual exclusion lock.

lock()
# Acquires a lock.

unlock()
# Releases a lock.

channel()
# Creates a communication channel.

send(channel, value)
# Sends data through a channel.

receive(channel)
# Receives data from a channel.

# ============================================================
# EDA DATABASE / QUERY HELPERS
# ============================================================

select(collection, query)
# Selects objects from a collection matching a query.

where(collection, condition)
# Filters a collection using a logical condition.

groupBy(collection, key)
# Groups objects by a specified property.

sortByField(collection, field)
# Sorts objects according to a field value.

aggregate(collection, fn)
# Combines collection values using an aggregation function.

bomCost(board)
# Calculates the estimated bill-of-materials cost.

componentCount(board)
# Returns the number of components on a board.

netClass(net)
# Returns the electrical class of a net.


# ============================================================
# ENGINEERING QUANTITY TYPES
# ============================================================

Voltage(value)
# Creates a voltage quantity with unit support.

Current(value)
# Creates a current quantity with unit support.

Resistance(value)
# Creates a resistance quantity with unit support.

Capacitance(value)
# Creates a capacitance quantity with unit support.

Inductance(value)
# Creates an inductance quantity with unit support.

Frequency(value)
# Creates a frequency quantity with unit support.

Power(value)
# Creates a power quantity with unit support.

Temperature(value)
# Creates a temperature quantity with unit support.

Length(value)
# Creates a length quantity with unit support.

Mass(value)
# Creates a mass quantity with unit support.

Time(value)
# Creates a time quantity with unit support.


# ============================================================
# ENGINEERING CALCULATIONS
# ============================================================

calculatePower(voltage, current)
# Calculates electrical power from voltage and current.

calculateEnergy(power, time)
# Calculates energy consumed over a time interval.

calculateReactance(frequency, component)
# Calculates capacitive or inductive reactance.

calculateAdmittance(impedance)
# Calculates the inverse of impedance.

calculateQFactor(component)
# Calculates quality factor of a resonant component.

calculateBandwidth(center, q)
# Calculates bandwidth from center frequency and quality factor.

thermalAnalysis(device)
# Performs thermal behavior calculations.

stressAnalysis(material, load)
# Performs mechanical stress calculations.


# ============================================================
# SIMULATION FRAMEWORK
# ============================================================

simulate(model, parameters)
# Runs a simulation using a model and parameters.

monteCarloSimulation(model, iterations)
# Runs repeated simulations with randomized parameters.

sensitivityAnalysis(model, parameter)
# Determines how output changes with parameters.

parameterSweep(model, parameter, range)
# Runs simulations across a range of values.

optimizeDesign(model, constraints)
# Finds an optimized design satisfying constraints.


# ============================================================
# DATA / SCIENCE HELPERS
# ============================================================

loadDataset(path)
# Loads a dataset from storage.

saveDataset(path, data)
# Saves a dataset to storage.

csvToMatrix(path)
# Converts CSV data into a numerical matrix.

matrixToCSV(matrix, path)
# Writes a matrix into CSV format.

plot(data)
# Creates a basic data visualization.

plotXY(x, y)
# Creates an X-Y plot.

plotHistogram(data)
# Creates a histogram visualization.

plotSpectrum(signal)
# Displays frequency spectrum information.


# ============================================================
# ADVANCED PCB / EDA AUTOMATION
# ============================================================

generateBOM(board)
# Generates a bill of materials from a PCB design.

compareBoards(board1, board2)
# Compares two PCB designs for differences.

findUnusedParts(board)
# Finds components not used in the design.

findUnusedPins(board)
# Finds unused component pins.

detectDuplicateReferences(board)
# Detects duplicate component reference designators.

annotateSchematic(schematic)
# Assigns component reference identifiers.

backAnnotate(board, schematic)
# Synchronizes PCB references back into schematic.

updateFootprints(board)
# Updates PCB footprints from component definitions.

updateSymbols(schematic)
# Updates schematic symbols from libraries.


# ============================================================
# HARDWARE INTERFACE HELPERS
# ============================================================

measureVoltage(pin)
# Reads voltage from a hardware measurement point.

measureCurrent(pin)
# Reads current from a hardware measurement point.

measureFrequency(pin)
# Measures signal frequency.

measureTemperature(sensor)
# Reads temperature from a sensor.

logicAnalyze(channel)
# Captures and analyzes digital signals.

oscilloscope(channel)
# Captures waveform data.

powerOn(device)
# Enables power to a device.

powerOff(device)
# Disables power to a device.


# ============================================================
# PCB MANUFACTURING
# ============================================================

generateGerber(board)
# Creates Gerber fabrication files.

generateDrillFile(board)
# Creates drill manufacturing data.

generatePickAndPlace(board)
# Creates assembly placement data.

generateStencil(board)
# Creates solder paste stencil data.

panelize(board, layout)
# Creates a manufacturing panel.

exportManufacturingPackage(board)
# Exports all fabrication files.


# ============================================================
# VISUALIZATION
# ============================================================

show(object)
# Displays an object visually.

render(object)
# Generates a rendered representation.

preview(board)
# Opens a PCB preview.

highlight(object)
# Highlights an object in a viewer.

hide(object)
# Hides an object.

zoom(area)
# Changes visualization zoom level.

pan(dx, dy)
# Moves the view position.


# ============================================================
# CONFIGURATION / ENVIRONMENT
# ============================================================

getConfig(name)
# Reads a configuration value.

setConfig(name, value)
# Sets a configuration value.

environment(name)
# Reads an environment variable.

setEnvironment(name, value)
# Sets an environment variable.

version()
# Returns language/runtime version.

features()
# Returns supported runtime features.


# ============================================================
# LOGGING / DEBUGGING
# ============================================================

assert(condition, message)
# Stops execution if a condition is false.

debug(value)
# Outputs debugging information.

trace(message)
# Records execution tracing information.

profile(function)
# Measures execution performance.

memoryUsage()
# Reports current memory usage.


# ============================================================
# ADVANCED MATH EXTENSIONS
# ============================================================

symbolic(expression)
# Creates a symbolic mathematical expression.

simplify(expression)
# Simplifies a symbolic expression.

differentiate(expression, variable)
# Performs symbolic differentiation.

integrateSymbolic(expression, variable)
# Performs symbolic integration.

solveEquation(expression, variable)
# Solves symbolic equations.


# ============================================================
# NULL / OPTIONAL OPERATORS
# ============================================================

??          # Null coalescing
            # a ?? b returns a if not null, otherwise b

?.          # Safe member access
            # obj?.member returns null if obj is null

?[]         # Safe indexing
            # array?[index]


# ============================================================
# PIPELINE / FUNCTIONAL OPERATORS
# ============================================================

|>          # Pipeline operator
            # value |> function
            #
            # Example:
            # data |> filter(valid) |> sort()


>>          # Function composition
            # f >> g creates g(f(x))


<<          # Reverse composition
            # f << g creates f(g(x))


# ============================================================
# RANGE OPERATORS
# ============================================================

..          # Inclusive/exclusive range

1..10       # 1 through 10

..          # Range creation

1..<10      # Explicit exclusive end

..<          # Exclusive range operator


# ============================================================
# IMMUTABILITY / BINDING
# ============================================================

:=          # Immutable binding

::=         # Constant declaration

?=          # Assign only if null


# ============================================================
# COLLECTION OPERATORS
# ============================================================

in          # Membership

not in      # Negated membership


|           # Union / merge

&           # Intersection

\           # Difference


# Example:

A | B       # combine collections

A & B       # common elements

A \ B       # remove elements


# ============================================================
# ARRAY / MATRIX OPERATORS
# ============================================================

@           # Matrix multiplication

            # A @ B


.*          # Element-wise multiplication

./          # Element-wise division

.^          # Element-wise power


T           # Transpose shorthand

            # A.T

H           # Hermitian transpose

            # A.H


# ============================================================
# COMPLEX NUMBER OPERATORS
# ============================================================

<|          # Create complex number

3 <| 4

            # equivalent:
            # complex(3,4)


∠           # Phase angle

z ∠         # phase of complex number


# ============================================================
# ENGINEERING UNIT OPERATORS
# ============================================================

~           # Approximate equality

            # 5V ~ 5.01V


±           # Tolerance operator

10kΩ ±5%


×           # Engineering multiplication

10 × 3


# ============================================================
# SIGNAL PROCESSING OPERATORS
# ============================================================

*           # Convolution context

a * b       # signal convolution if signal objects


⊗           # Tensor product


⊕           # Signal addition / XOR depending on type


# ============================================================
# LOGIC OPERATORS
# ============================================================

&&          # Logical AND

||          # Logical OR

!           # Logical NOT


^^          # Logical XOR


# ============================================================
# BITWISE OPERATORS
# ============================================================

&           # Bitwise AND

|           # Bitwise OR

^           # Bitwise XOR

~           # Bitwise NOT

<<          # Shift left

>>          # Shift right


# ============================================================
# POINTER / REFERENCE OPERATORS
# ============================================================

&x          # Reference/address of x

*x          # Dereference x


# ============================================================
# PATTERN MATCHING
# ============================================================

match       # Pattern matching operator


value match
{
    0       => "zero"
    1..10   => "small"
    _       => "other"
}


?=          # Pattern assignment


# ============================================================
# EDA-SPECIFIC OPERATORS
# ============================================================

->          # Connection operator

R1.pin1 -> net.VCC


<->         # Bidirectional connection

A <-> B


~>          # Signal flow

sensor ~> filter ~> output


@           # Object query selector

board@R1

board@net("GND")


# ============================================================
# GEOMETRY OPERATORS
# ============================================================

||          # Parallel geometry

⊥           # Perpendicular


∩           # Intersection

∪           # Union


# ============================================================
# SIMULATION OPERATORS
# ============================================================

~           # Approximate simulation result


=>          # Transformation / mapping


?           # Parameter sweep marker


# Example:

R = 1kΩ?
# variable parameter


# ============================================================
# TYPE OPERATORS
# ============================================================

is          # Type checking

as          # Type conversion


x is Matrix

x as Float


# ============================================================
# OPTIONAL CHAINING
# ============================================================

?.          # Safe call

obj?.method()


?()         # Safe function call

fn?()


# ============================================================
# EVALUATION CONTROL
# ============================================================

??=         # Null assignment

x ??= value


:=          # Lazy binding

name := expensiveCalculation()


# ============================================================
# POWERFUL ADDITION: UNITS AS OPERATORS
# ============================================================

5V          # Voltage literal

10kΩ        # Resistance literal

2.4GHz      # Frequency literal

100nF       # Capacitance literal


# ============================================================
# PROPOSED FINAL CORE OPERATOR SET
# ============================================================

Arithmetic:
+ - * / % **

Comparison:
== != < <= > >= ~

Logical:
&& || !

Bitwise:
& | ^ ~ << >>

Assignment:
= := ::= ??=

Navigation:
. ?. ?[]

Range:
.. ..<

Pipeline:
|>

Composition:
>> <<

Collections:
in | & \

Matrix:
@ .* ./ .^ .T .H

Pattern:
match =>

EDA:
-> <-> ~>

Units:
±






Arithmetic
----------
abs
sign
min
max
clamp
gcd
lcm
pow
root
factorial
doubleFactorial
fallingFactorial
risingFactorial
binomial
multinomial
modPow
modInverse
divides

Prime Numbers
-------------
isPrime
nextPrime
previousPrime
nearestPrime
nthPrime
primePi
primeFactors
distinctPrimeFactors
omega
bigOmega
totient
mobius
liouville
jacobiSymbol
legendreSymbol
kroneckerSymbol
carmichaelLambda

Integer Sequences
-----------------
fibonacci
lucas
pell
tribonacci
catalan
bell
bernoulli
eulerNumber
stirling1
stirling2
partitionNumber
harmonicNumber

Number Theory
-------------
continuedFraction
continuedFractionValue
isPerfect
isAbundant
isDeficient
isSquareFree
isPower
integerSqrt
valuation
chineseRemainder
primitiveRoot

Elementary Functions
--------------------
sqrt
cbrt
exp
log
log2
log10
expm1
log1p

Trigonometry
------------
sin
cos
tan
cot
sec
csc
asin
acos
atan
atan2
acot
asec
acsc

Hyperbolic
-----------
sinh
cosh
tanh
coth
sech
csch
asinh
acosh
atanh
acoth
asech
acsch

Special Functions
-----------------
gamma
beta
digamma
polygamma
erf
erfc
dawson
fresnelS
fresnelC
expIntegral
logIntegral
sineIntegral
cosineIntegral
hyperbolicSineIntegral
hyperbolicCosineIntegral
airyAi
airyBi
besselJ
besselY
besselI
besselK
hankelH1
hankelH2
kelvinBer
kelvinBei
kelvinKer
kelvinKei
struveH
struveL
polylog
dilog
trilog
lerchPhi
hurwitzZeta
riemannZeta
dirichletEta
lambertW
hypergeometric0F1
hypergeometric1F1
hypergeometric2F1
hypergeometricPFQ
meijerG
foxH
jacobiTheta
ellipticK
ellipticE
ellipticPi
jacobiSN
jacobiCN
jacobiDN
weierstrassP
incompleteGamma
incompleteBeta

Orthogonal Polynomials
----------------------
legendre
associatedLegendre
chebyshevT
chebyshevU
gegenbauer
jacobiPolynomial
laguerre
hermite
zernike

Complex Analysis
----------------
real
imag
arg
conjugate
cis
polar
rectangular

Linear Algebra
--------------
dot
cross
norm
normalize
det
trace
transpose
inverse
rank
eigenvalues
eigenvectors
svd
qr
lu
cholesky

Calculus
---------
derivative
partialDerivative
gradient
divergence
curl
laplacian
integral
limit
series
taylor
laurent

Numerical Analysis
------------------
newton
bisection
secant
brent
interpolate
spline
integrateAdaptive
differentiateNumerically
odeSolve
fft
ifft

Statistics
----------
mean
median
mode
variance
stddev
covariance
correlation
percentile
quantile

Probability Distributions
-------------------------
normalPDF
normalCDF
normalInvCDF
normalRandom

binomialPMF
binomialCDF
binomialInvCDF
binomialRandom

poissonPMF
poissonCDF
poissonInvCDF
poissonRandom

exponentialPDF
exponentialCDF
exponentialInvCDF
exponentialRandom

gammaPDF
gammaCDF
gammaInvCDF
gammaRandom

betaPDF
betaCDF
betaInvCDF
betaRandom

weibullPDF
weibullCDF
weibullInvCDF
weibullRandom

logisticPDF
logisticCDF
logisticInvCDF
logisticRandom

cauchyPDF
cauchyCDF
cauchyInvCDF
cauchyRandom

studentTPDF
studentTCDF
studentTInvCDF
studentTRandom

chiSquarePDF
chiSquareCDF
chiSquareInvCDF
chiSquareRandom

fDistributionPDF
fDistributionCDF
fDistributionInvCDF
fDistributionRandom

uniformPDF
uniformCDF
uniformInvCDF
uniformRandom

geometricPMF
geometricCDF
geometricInvCDF
geometricRandom

negativeBinomialPMF
negativeBinomialCDF
negativeBinomialInvCDF
negativeBinomialRandom

Combinatorics
-------------
permutations
combinations
partitions
compositions
grayCode
derangements

Geometry
--------
distance
angle
area
volume
centroid
convexHull
intersect
rotate
reflect

Symbolic Mathematics
--------------------
expand
factor
simplify
collect
substitute
solve
differentiate
integrate
seriesExpand

Miscellaneous
-------------
isFinite
isNaN
ulp
epsilon
random
randomInteger
randomPrime




f.os f.db f.vis f.thrd f.net f.color any other namespace suggestions? 
